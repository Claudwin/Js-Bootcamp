const account = {
    name: 'Claude Fortune',
    expenses: [],
    addExpense = function (description, amount) {
        this.account.push({
            description: description,
            amount: amount
        })
}, 
    getAccountSummary: function () {
        
    }

}